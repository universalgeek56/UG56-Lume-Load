// Config.h
#pragma once

// Wi-Fi settings
#define WIFI_SSID      "Your-SSID"          // Wi-Fi network SSID
#define WIFI_PASSWORD  "Your-Pass"    // Wi-Fi network password

// OTA settings
#define OTA_HOSTNAME   "LumeLoad"         // Hostname for OTA updates

// LED strip settings
#define LED_PIN        2               // GPIO2 for LED strip (e.g., D4 on ESP8266)
#define NUMPIXELS      60              // Number of LEDs in strip
